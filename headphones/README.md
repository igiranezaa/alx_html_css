# Headphones Landing Page

This project is part of the **ALX Front-End Program**.  
The goal is to recreate a pixel-perfect, responsive landing page for a headphones brand based on a Figma design, using only **HTML** and **CSS** (and a small bit of vanilla JavaScript in the last task).

---

## 🎧 Project Description

The page is a marketing/landing page for a headphones product. It includes:

- A hero **header** with logo, navigation and a call-to-action button  
- A **“What we do…”** features section  
- An **“Our results”** metrics section  
- A **Contact** form  
- A **Footer** with logo and social links  
- A **mobile layout** for screens `480px` and below  
- A **hamburger menu** on mobile (final task)

The objective is to match the designer’s Figma as closely as possible while keeping the code clean, semantic and maintainable.

---

## 📁 Repository Structure

- **Repository:** `alx_html_css`  
- **Directory:** `headphones/`

Main project files (per task):

- `0-index.html`, `0-styles.css` – Header & hero section  
- `1-index.html`, `1-styles.css` – + “What we do…” section  
- `2-index.html`, `2-styles.css` – + “Our results” section  
- `3-index.html`, `3-styles.css` – + Contact form  
- `4-index.html`, `4-styles.css` – + Footer (full page)  
- `6-index.html`, `6-styles.css` – CSS-only pentagons for “Our results”  
- `7-index.html`, `7-styles.css` – Animations on “What we do…” & “Our results”  
- `8-index.html`, `8-styles.css`, `8-script.js` – Hamburger menu for mobile

Assets:

- `images/` – Backgrounds, logos, product images  
- `holberton_school-icon/` – Custom icon font  
- `fonts/` – Optional local fonts (`Source Sans Pro`, `Spin Cycle OT`), if needed

---

## 🛠 Technologies

- **HTML5**
- **CSS3**
- **Vanilla JavaScript** (only for the hamburger menu in Task 8)  
- No CSS frameworks (no Bootstrap, Tailwind, etc.)

---

## 🎯 Key Requirements

- No external CSS frameworks
- No JavaScript except for Task 8
- **Max content width:** `1000px`, centered
- **Mobile breakpoint:** `480px`
- Hover / active styles:
  - Links: `#FF6565`
  - Buttons: `opacity: 0.9`

---

## ♿ Accessibility & Semantics

- Use semantic tags: `header`, `nav`, `main`, `section`, `footer`
- Proper use of headings (`h1`, `h2`, …)
- `alt` attributes on images
- Labeled form inputs
- Visible focus states for keyboard users

---

## 📱 Responsiveness

- Use media queries at `max-width: 480px`
- Switch layout to match the **mobile Figma** design
- Ensure text remains readable and tap targets are large enough

---


